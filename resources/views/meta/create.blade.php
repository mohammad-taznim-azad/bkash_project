@extends('layouts.main')
@section('title'){{ 'Meta Create' }}@endsection
@section('header.css')
    <style>

    </style>
@endsection
@section('main.content')
    <div class="page-body">
        <div class="container-fluid">
            <div class="page-title">
                <div class="row">
                    <div class="col-6">
                        <h3>Meta Create</h3>
                    </div>
                    <div class="col-6">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item">
                                <a href="{{ route('index') }}">
                                    <i class="fa fa-home"></i>
                                </a>
                            </li>
                            <li class="breadcrumb-item">Settings</li>
                            <li class="breadcrumb-item active">Meta</li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
        <!-- Container-fluid starts-->
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-body">
                            <form class="form-wizard" action="{{ route('meta.store') }}" method="POST" enctype="multipart/form-data">
                                @csrf
                                <div class="row ">
                                    <div class="col-md-8">
                                        <div class="mb-3">
                                            <label for="metaName">Meta Name</label>
                                            <input class="form-control" id="metaName" name="metaName" type="text" placeholder="Meta Name" value="{{ old('metaName') }}" required>
                                            <span class="text-danger"><b>{{  $errors->first('metaName') }}</b></span>
                                        </div>
                                        <div class="mb-3">
                                            <label for="metaContent">Meta Content</label>
                                            <input class="form-control" id="metaContent" name="metaContent" type="text" placeholder="Meta Content" value="{{ old('metaContent') }}" required>
                                            <span class="text-danger"><b>{{  $errors->first('metaContent') }}</b></span>
                                        </div>
                                        <div class="text-end btn-mb">
                                            <button class="btn btn-secondary" type="button"><a class="text-white" href="{{ route('meta.show') }}">Cancel</a></button>
                                            <button class="btn btn-primary" type="submit">Create</button>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
